# تحديث إزالة الكروب

هذه الحزمة تزيل واجهة كروب «نورانيات كاظم الغيظ» من التطبيق، وتعيد التطبيق إلى واجهة الختمات فقط. لم تُحذف بيانات الكروب القديمة من Firebase؛ بقيت محفوظة احتياطًا ولا تُقرأ من التطبيق بعد هذا التحديث.

## الملفات التي تُستبدل

استبدل `index.html` و`app.js` و`sw.js` في مستودع GitHub Pages. واستبدل قواعد Firebase كاملة بمحتوى `database.rules.json`. لا تستبدل مكتبات Firebase أو `offline-store.js` أو الخطوط والأيقونات.

## ملاحظة

تم إبقاء تحسين فهرس الختمات `khatmaIndex` لتقليل استهلاك Firebase. لا تحذف مسار `khatmas` من قاعدة البيانات.


## إصلاح عدم ظهور النتائج

إذا لم يكن مسار `khatmaIndex` موجودًا بعد، تحاول النسخة الجديدة قراءة المسار القديم `khatmas` تلقائيًا حتى لا تظهر صفحة فارغة. إذا كانت قاعدة Firebase متوقفة بسبب تجاوز الحصة، يجب انتظار عودة الخدمة أو معالجة حد الاستخدام؛ لا يمكن لأي نسخة من التطبيق قراءة البيانات أثناء توقف قاعدة البيانات.
